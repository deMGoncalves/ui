import css from '@standard/css'

const style = css`
  .form {
    align-items: start;
    display: flex;
    flex-direction: column;
    gap: var(--spacing_inset-md);
    max-width: 768px;
    width: 100%;
  }
`

export default style
